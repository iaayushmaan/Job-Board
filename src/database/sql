> create database testdb;
> use testdb;
> CREATE TABLE postings (

      id INT AUTO_INCREMENT PRIMARY KEY,
      title VARCHAR(255),
      company VARCHAR(255),
       location VARCHAR(255),
      salary DECIMAL(10, 2),
       description TEXT
     );
